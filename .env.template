# URL

# Local Dev
# ORIGIN=http://localhost:5173
# Docker
ORIGIN=http://localhost:3000
VITE_ENV=development

# Frontend Variables
VITE_SITE_NAME="Vanilla Cookbook"

# Maximum file upload size in bytes
BODY_SIZE_LIMIT=5242880

# Absolute File path to project folder
# **You probably don't need to worry about this unless you're going to be importing via the Paprika API**
VITE_APP_ROOT_PATH=/Your/Full/Path/To/Production/Folder/

# Open AI
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxx
LLM_API_ENABLED=true
LLM_PROVIDER=openai
# Optional overrides per stream
LLM_TEXT_PROVIDER=
LLM_IMAGE_PROVIDER=
LLM_TEXT_MODEL=gpt-3.5-turbo
LLM_IMAGE_MODEL=gpt-4o

# Generic API key (fallback) or provider-specific keys
LLM_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Ingredient Matching (Fuse.js Configuration)
# Threshold for strict ingredient matching (0.0 = perfect match, 1.0 = match anything)
# Lower values = more strict matching, fewer false positives
FUSE_THRESHOLD_STRICT=0.3

# Threshold for relaxed matching (used for individual words in multi-word ingredients)
# Higher than strict threshold to catch variations like "yellow onions" → "onions"
FUSE_THRESHOLD_RELAXED=0.5

# Minimum length of a word to attempt matching (prevents matching articles like "of", "in")
FUSE_MIN_WORD_LENGTH=3

# Distance parameter: how far from the start of the text can a match be?
# Lower = matches must be closer to the beginning, Higher = matches can be anywhere
FUSE_DISTANCE=100

# Minimum characters that must match for a valid result
FUSE_MIN_MATCH_CHAR_LENGTH=2

# OAuth Providers
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Registration
REGISTRATION_ALLOWED=true

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_LOGIN_PER_IP=10
RATE_LIMIT_LOGIN_PER_ID=5

# Database Backups (Docker only)
# Cron schedule for automated backups (default: Sundays at 3am)
# Format: "minute hour day month weekday"
# Examples:
#   0 3 * * 0    = Every Sunday at 3am (default)
#   0 2 * * *    = Every day at 2am
#   0 4 * * 1    = Every Monday at 4am
#   0 3 1 * *    = 1st of every month at 3am
BACKUP_CRON_SCHEDULE=0 3 * * 0

# Number of scheduled backups to keep (default: 6 = 6 weeks for weekly backups)
BACKUP_RETENTION_COUNT=6
