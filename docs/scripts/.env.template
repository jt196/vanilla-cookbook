#
# Sample environment for docs/scripts/screenshots.py
# Copy to `.env` in this folder and fill in your values.
#

# Base URL of the running app (e.g. local dev server)
ORIGIN=http://localhost:5173

# Admin credentials to log in before capturing
ADMIN_USER=your_admin_username
ADMIN_PASSWORD=your_admin_password
ADMIN_ID=your_admin_user_id

# Optional: set to 1/true/yes/on to capture only the first recipe page
# ONLY_RECIPE=1

# Optional: location for Playwright browser binaries; omit to use defaults
# PLAYWRIGHT_BROWSERS_PATH=node_modules/.cache/ms-playwright
